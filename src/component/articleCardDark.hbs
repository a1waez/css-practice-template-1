<div class="articles_seccond">
    <img src="{{photo}}" alt="articles" class="articles_photo" id="{{id}}">

    {{#if isNew}}
    <span class="article_badge">NEW</span>
    {{else}}
    {{/if}}
    <strong class="articles_name">{{name}}</strong>
    <p class="articles_description">{{description}}</p>
    <a href="#" class="articles_button">Read All</a>
</div>